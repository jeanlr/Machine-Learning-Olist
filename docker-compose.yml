networks:
  data_network:
    driver: bridge

volumes:
  minio_data:
  postgres_data:
  spark_data:
  #airflow_logs:
  notebooks:

services:
  # ==================== MINIO (Object Storage) ====================
  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - data_network

  # ==================== POSTGRES (para Airflow) ====================
  postgres:
    image: postgres:13
    container_name: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U airflow"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - data_network

  # ==================== SPARK MASTER ====================
  spark-master:
    build:
      context: ./spark
      dockerfile: Dockerfile
    container_name: spark-master
    ports:
      - "8083:8080"
      - "7077:7077"
    environment:
      - SPARK_MODE=master
      - SPARK_MASTER_HOST=spark-master
      - SPARK_MASTER_PORT=7077
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=http://minio:9000
    volumes:
      - spark_data:/opt/spark/data
      - ./scripts:/opt/spark/scripts
      - ./spark/entrypoint.sh:/opt/entrypoint.sh
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - data_network

  # ==================== SPARK WORKERS ====================
  spark-worker-1:
    build:
      context: ./spark
      dockerfile: Dockerfile
    container_name: spark-worker-1
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=2g
      - SPARK_WORKER_CORES=2
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=http://minio:9000
    volumes:
      - spark_data:/opt/spark/data
      - ./scripts:/opt/spark/scripts
      - ./spark/entrypoint.sh:/opt/entrypoint.sh
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - data_network

  spark-worker-2:
    build:
      context: ./spark
      dockerfile: Dockerfile
    container_name: spark-worker-2
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=2g
      - SPARK_WORKER_CORES=2
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=http://minio:9000
    volumes:
      - spark_data:/opt/spark/data
      - ./scripts:/opt/spark/scripts
      - ./spark/entrypoint.sh:/opt/entrypoint.sh
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - data_network

  spark-worker-3:
    build:
      context: ./spark
      dockerfile: Dockerfile
    container_name: spark-worker-3
    environment:
      - SPARK_MODE=worker
      - SPARK_MASTER_URL=spark://spark-master:7077
      - SPARK_WORKER_MEMORY=2g
      - SPARK_WORKER_CORES=2
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - MINIO_ENDPOINT=http://minio:9000
    volumes:
      - spark_data:/opt/spark/data
      - ./scripts:/opt/spark/scripts
      - ./spark/entrypoint.sh:/opt/entrypoint.sh
    depends_on:
      spark-master:
        condition: service_healthy
    networks:
      - data_network

  # ==================== AIRFLOW ====================
  # airflow-init:
  #   build:
  #     context: ./airflow
  #     dockerfile: Dockerfile
  #   container_name: airflow-init
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   environment:
  #     - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
  #     - AIRFLOW__CORE__EXECUTOR=LocalExecutor
  #   entrypoint: /bin/bash
  #   command: -c "airflow db init && airflow users create --username ${AIRFLOW_ADMIN_USERNAME} --firstname Admin --lastname User --role Admin --email ${AIRFLOW_ADMIN_EMAIL} --password ${AIRFLOW_ADMIN_PASSWORD} || true"
  #   networks:
  #     - data_network

  # airflow-webserver:
  #   build:
  #     context: ./airflow
  #     dockerfile: Dockerfile
  #   container_name: airflow-webserver
  #   ports:
  #     - "8081:8080"
  #   environment:
  #     - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
  #     - AIRFLOW__CORE__EXECUTOR=LocalExecutor
  #     - AIRFLOW__CORE__FERNET_KEY=${FERNET_KEY}
  #     - AIRFLOW__WEBSERVER__DEFAULT_UI_TIMEZONE=UTC
  #     - MINIO_ENDPOINT=http://minio:9000
  #     - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
  #     - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
  #   # Spark Configuration
  #     - SPARK_MASTER=${SPARK_MASTER}
  #     - SPARK_DRIVER_HOST=${SPARK_DRIVER_HOST}
      
  #     # Python paths
  #     - PYTHONPATH=/opt/airflow:/opt/airflow/scripts
  #   volumes:
  #     - ./airflow/dags:/opt/airflow/dags
  #     - ./airflow/logs:/opt/airflow/logs
  #     - ./airflow/plugins:/opt/airflow/plugins
  #     - ./scripts:/opt/airflow/scripts
  #   command: webserver
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     airflow-init:
  #       condition: service_completed_successfully
  #     spark-master:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy
  #   networks:
  #     - data_network

  # airflow-scheduler:
  #   build:
  #     context: ./airflow
  #     dockerfile: Dockerfile
  #   container_name: airflow-scheduler
  #   environment:
  #     - AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres/airflow
  #     - AIRFLOW__CORE__EXECUTOR=LocalExecutor
  #     - AIRFLOW__CORE__FERNET_KEY=${FERNET_KEY}
  #     - MINIO_ENDPOINT=http://minio:9000
  #     - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
  #     - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
  #     # Spark Configuration
  #     - SPARK_MASTER=${SPARK_MASTER}
  #     - SPARK_DRIVER_HOST=${SPARK_DRIVER_HOST}
      
  #     # Python paths
  #     - PYTHONPATH=/opt/airflow:/opt/airflow/scripts
  #   volumes:
  #     - ./airflow/dags:/opt/airflow/dags
  #     - ./airflow/logs:/opt/airflow/logs
  #     - ./airflow/plugins:/opt/airflow/plugins
  #     - ./scripts:/opt/airflow/scripts
  #   command: scheduler
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     airflow-init:
  #       condition: service_completed_successfully
  #     spark-master:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy
  #   networks:
  #     - data_network

  # ==================== JUPYTER ====================
  jupyter:
    build:
      context: ./jupyter
      dockerfile: Dockerfile
    container_name: jupyter
    ports:
      - "8888:8888"
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - MINIO_ENDPOINT=http://minio:9000
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
      - SPARK_MASTER=spark://spark-master:7077
      - SPARK_DRIVER_HOST=jupyter
    volumes:
      - notebooks:/home/jovyan/work 
      - ./scripts:/home/jovyan/scripts
      - spark_data:/home/jovyan/data
    command: start-notebook.sh --NotebookApp.token=''
    depends_on:
      spark-master:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - data_network

  # ==================== MINIO BUCKET INITIALIZER ====================
  minio-init:
    image: python:3.10-slim
    container_name: minio-init
    depends_on:
      minio:
        condition: service_healthy
    environment:
      - MINIO_ENDPOINT=http://minio:9000
      - AWS_ACCESS_KEY_ID=${MINIO_ROOT_USER}
      - AWS_SECRET_ACCESS_KEY=${MINIO_ROOT_PASSWORD}
    volumes:
      - ./scripts:/scripts
    command: >
      sh -c "pip install --no-cache-dir boto3 &&
             python /scripts/init_minio.py"
    networks:
      - data_network